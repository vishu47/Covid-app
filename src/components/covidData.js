import React from 'react'

export default function covidData(data) {
    return (
        <>
            {
                // data.map(item => <li>{item}</li>)
            }        
        </>
    )
}
